import Link from"next/link";import{AdminList}from"@/components/admin/AdminList";import{dateCell,parseAdminParams}from"@/lib/admin-page";import{getAdminRows}from"@/services/admin/admin-data.service";
export default async function Page({searchParams}:{searchParams:Promise<Record<string,string|undefined>>}){const p=await parseAdminParams(searchParams);const d=await getAdminRows('resources',{...p,searchColumn:'title'});return <AdminList title="Tài liệu" {...d} {...p} statuses={['draft','published','archived']} createHref="/admin/resources/new" columns={[{key:'title',label:'Tên',format:(v,r)=><Link className="font-semibold text-brand-blue" href={`/admin/resources/${r.id}`}>{String(v)}</Link>},{key:'resource_type',label:'Loại'},{key:'access_type',label:'Truy cập'},{key:'status',label:'Trạng thái'},{key:'is_featured',label:'Featured',format:v=>v?'Có':'Không'},{key:'updated_at',label:'Cập nhật',format:dateCell}]}/>}
