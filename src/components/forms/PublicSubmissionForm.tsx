"use client";
import { useActionState } from "react";
import { useFormStatus } from "react-dom";
import type { PublicCourseOption, PublicFormState } from "@/types/support";
type Kind="contact"|"registration"|"job";const initial:PublicFormState={status:"idle"};
function Submit({label}:{label:string}){const{pending}=useFormStatus();return <button disabled={pending} className="button-primary w-full disabled:opacity-60">{pending?"Đang gửi...":label}</button>}
function Area({name,label,value,error}:{name:string;label:string;value:string;error?:string}){return <div className="sm:col-span-2"><label htmlFor={name} className="text-sm font-semibold">{label}</label><textarea id={name} name={name} defaultValue={value} rows={4} className="mt-2 w-full rounded-lg border border-slate-300 px-3 py-2" aria-invalid={Boolean(error)}/>{error&&<p className="mt-1 text-sm text-red-700">{error}</p>}</div>}
export function PublicSubmissionForm({action,kind,courses=[],selectedCourse,label}:{action:(s:PublicFormState,d:FormData)=>Promise<PublicFormState>;kind:Kind;courses?:PublicCourseOption[];selectedCourse?:string;label:string}){const[state,formAction]=useActionState(action,initial);const error=(key:string)=>state.fieldErrors?.[key]?.[0];const value=(key:string)=>state.values?.[key]??"";const field=(name:string,text:string,type="text")=><div><label htmlFor={name} className="text-sm font-semibold">{text}</label><input id={name} name={name} type={type} defaultValue={value(name)} className="mt-2 min-h-11 w-full rounded-lg border border-slate-300 px-3" aria-invalid={Boolean(error(name))}/>{error(name)&&<p className="mt-1 text-sm text-red-700">{error(name)}</p>}</div>;return <form action={formAction} className="grid gap-5 rounded-2xl border border-slate-200 bg-white p-6 shadow-sm sm:grid-cols-2" noValidate>{state.message&&<p role="alert" className={`sm:col-span-2 rounded-lg p-3 text-sm ${state.status==="database_error"?"bg-red-50 text-red-800":"bg-amber-50 text-amber-900"}`}>{state.message}</p>}{kind==="registration"&&<><div className="sm:col-span-2"><label htmlFor="course_id" className="text-sm font-semibold">Khóa học *</label><select id="course_id" name="course_id" defaultValue={value("course_id")||selectedCourse||""} className="mt-2 min-h-11 w-full rounded-lg border px-3"><option value="">Chọn khóa học</option>{courses.map(c=><option key={c.id} value={c.id}>{c.title}</option>)}</select>{error("course_id")&&<p className="text-sm text-red-700">{error("course_id")}</p>}</div><div className="sm:col-span-2"><label htmlFor="learner_type" className="text-sm font-semibold">Người học *</label><select id="learner_type" name="learner_type" defaultValue={value("learner_type")||"self"} className="mt-2 min-h-11 w-full rounded-lg border px-3"><option value="self">Bản thân</option><option value="child">Con/em trong gia đình</option><option value="other">Người khác</option></select></div></>}{field("full_name","Họ và tên *")}{field("phone","Số điện thoại *","tel")}{field("email","Email","email")}{kind==="contact"&&field("subject","Chủ đề *")}{kind==="contact"&&<Area name="message" label="Nội dung *" value={value("message")} error={error("message")}/>} {kind==="registration"&&<><Area name="current_level" label="Trình độ hiện tại" value={value("current_level")} error={error("current_level")}/><Area name="learning_goal" label="Mục tiêu học tập *" value={value("learning_goal")} error={error("learning_goal")}/><Area name="note" label="Ghi chú" value={value("note")} error={error("note")}/></>}{kind==="job"&&<Area name="cover_letter" label="Thư giới thiệu ngắn" value={value("cover_letter")} error={error("cover_letter")}/>}<div className="sm:col-span-2"><label className="flex gap-3 text-sm"><input type="checkbox" name="privacy_accepted" required className="mt-1 size-4"/><span>Tôi đồng ý để Vũ Thịnh sử dụng thông tin này nhằm xử lý yêu cầu. *</span></label>{error("privacy_accepted")&&<p className="text-sm text-red-700">{error("privacy_accepted")}</p>}</div><div className="sm:col-span-2"><Submit label={label}/></div></form>}
