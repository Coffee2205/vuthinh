import{RequestTable}from"@/components/admin/RequestTable";import{parseAdminParams}from"@/lib/admin-page";import{getAdminRows}from"@/services/admin/admin-data.service";const statuses=['new','contacted','confirmed','completed','cancelled'] as const;
export default async function Page({searchParams}:{searchParams:Promise<Record<string,string|undefined>>}){const p=await parseAdminParams(searchParams);const d=await getAdminRows('consultation_requests',{...p,searchColumn:'full_name'});return <><h1 className="text-3xl font-bold">Yêu cầu tư vấn</h1><form className="mt-6 flex gap-3 rounded-xl border bg-white p-4"><input name="q" defaultValue={p.q} placeholder="Tên người yêu cầu…" className="min-h-11 flex-1 rounded-lg border px-3"/><select name="status" defaultValue={p.status} className="min-h-11 rounded-lg border px-3"><option value="">Tất cả</option>{statuses.map(s=><option key={s}>{s}</option>)}</select><button className="rounded-lg border px-4 font-semibold">Lọc</button></form><div className="mt-6"><RequestTable rows={d.rows} kind="consultation" statuses={statuses}/></div></>}
