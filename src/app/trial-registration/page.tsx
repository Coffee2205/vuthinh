import type{Metadata}from"next";import{Container}from"@/components/common/Container";import{PublicSubmissionForm}from"@/components/forms/PublicSubmissionForm";import{submitCourse,submitTrial}from"@/app/support-actions";import{getPublishedCourseOptions}from"@/services/support.service";export const metadata:Metadata={title:"Đăng ký học thử | Vũ Thịnh"};export const dynamic="force-dynamic";export default async function Page({searchParams}:{searchParams:Promise<{course?:string;type?:string;submitted?:string}>}){const params=await searchParams;const courses=await getPublishedCourseOptions();const selected=courses.find(c=>c.slug===params.course)?.id;const isCourse=params.type==="course";return <main><section className="bg-amber-50 py-16"><Container><h1 className="text-4xl font-bold sm:text-5xl">{isCourse?"Đăng ký khóa học":"Đăng ký học thử"}</h1><p className="mt-5 text-lg text-slate-600">Thông tin gửi đi là yêu cầu đăng ký; đội ngũ sẽ liên hệ xác nhận.</p></Container></section><section className="py-14"><Container className="max-w-3xl">{params.submitted?<p role="status" className="rounded-xl bg-green-50 p-5 text-green-800">Đăng ký đã được ghi nhận.</p>:<PublicSubmissionForm action={isCourse?submitCourse:submitTrial} kind="registration" courses={courses} selectedCourse={selected} label={isCourse?"Gửi đăng ký khóa học":"Gửi đăng ký học thử"}/>}</Container></section></main>}
