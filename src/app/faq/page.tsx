import type{Metadata}from"next";import Link from"next/link";import{Container}from"@/components/common/Container";import{JsonLd}from"@/components/common/JsonLd";import{createPageMetadata}from"@/lib/seo";import{getActiveFaqs}from"@/services/support.service";export const metadata:Metadata=createPageMetadata({title:"Câu hỏi thường gặp",description:"Giải đáp những câu hỏi thường gặp về chương trình học, đăng ký và tư vấn tại Vũ Thịnh.",path:"/faq"});export const dynamic="force-dynamic";export default async function Page({searchParams}:{searchParams:Promise<{category?:string}>}){const{category}=await searchParams;const{categories,faqs}=await getActiveFaqs();const active=categories.find(c=>c.slug===category);const shown=active?faqs.filter(f=>f.category_id===active.id):category?[]:faqs;const structuredData={"@context":"https://schema.org","@type":"FAQPage",mainEntity:shown.map(f=>({"@type":"Question",name:f.question,acceptedAnswer:{"@type":"Answer",text:f.answer}}))};return <main>{shown.length>0&&<JsonLd data={structuredData}/>}<section className="bg-emerald-50 py-16"><Container><h1 className="text-4xl font-bold sm:text-5xl">Câu hỏi thường gặp</h1></Container></section><section className="py-14"><Container><nav className="flex flex-wrap gap-2" aria-label="Danh mục FAQ"><Link className="button-primary" href="/faq">Tất cả</Link>{categories.map(c=><Link key={c.id} href={`/faq?category=${c.slug}`} className="inline-flex min-h-11 items-center rounded-lg border px-4">{c.name}</Link>)}</nav><div className="mt-8 grid gap-3">{shown.map(f=><details key={f.id} className="rounded-2xl border border-slate-200 p-5"><summary className="cursor-pointer font-bold focus-visible:outline-2">{f.question}</summary><p className="mt-4 leading-7 text-slate-600">{f.answer}</p></details>)}{!shown.length&&<p className="rounded-2xl bg-slate-50 p-6">Chưa có câu hỏi trong danh mục này.</p>}</div></Container></section></main>}
