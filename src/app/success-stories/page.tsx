import type{Metadata}from"next";import{Container}from"@/components/common/Container";import{getPublishedSuccessStories}from"@/services/support.service";export const metadata:Metadata={title:"Thành quả học viên",description:"Những câu chuyện học tập đã được đồng ý công bố tại Vũ Thịnh."};export const dynamic="force-dynamic";export default async function Page(){const stories=await getPublishedSuccessStories();return <main><section className="bg-blue-50 py-16"><Container><h1 className="text-4xl font-bold text-slate-950 sm:text-5xl">Thành quả học viên</h1><p className="mt-5 max-w-3xl text-lg leading-8 text-slate-600">Chỉ hiển thị câu chuyện đã xuất bản và được xác nhận đồng ý công bố.</p></Container></section><section className="py-14"><Container>{stories.length?<div className="grid gap-6 md:grid-cols-2">{stories.map(s=><article key={s.id} className="rounded-2xl border border-slate-200 p-6"><h2 className="text-2xl font-bold text-slate-950">{s.title}</h2>{(s.description||s.content)&&<p className="mt-4 whitespace-pre-line leading-7 text-slate-600">{s.description||s.content}</p>}{s.quote&&<blockquote className="mt-5 border-l-4 border-brand-green pl-4 italic text-slate-700">{s.quote}</blockquote>}</article>)}</div>:<div className="rounded-2xl border border-dashed border-slate-300 bg-slate-50 p-8"><h2 className="text-2xl font-bold">Câu chuyện đang được cập nhật</h2><p className="mt-3 text-slate-600">Chưa có thành quả đã xác nhận quyền công bố.</p></div>}</Container></section></main>}
