import { getDashboardData } from "@/services/admin/admin-data.service";
export default async function Dashboard(){const d=await getDashboardData();const cards=[['Tổng khóa học',d.courses],['Khóa học published',d.publishedCourses],['Tổng bài viết',d.posts],['Bài viết draft',d.draftPosts],['Tổng tài liệu',d.resources],['Tổng cảm nhận',d.stories],['Đăng ký mới',d.newRegistrations],['Tư vấn mới',d.consultations]];return <><h1 className="text-3xl font-bold">Dashboard</h1><div className="mt-6 grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{cards.map(([label,value])=><article key={label} className="rounded-xl border bg-white p-5"><p className="text-sm text-slate-600">{label}</p><p className="mt-2 text-3xl font-bold">{value}</p></article>)}</div></>}
